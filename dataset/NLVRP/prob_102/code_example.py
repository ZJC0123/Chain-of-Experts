def prob_102(VehicleCapacity, CustomerDemands, DepotLocations, CustomerLocations, CustomerTimeWindows):
    """
    Args:
        VehicleCapacity: an integer, indicates the maximum cargo capacity of each vehicle
        CustomerDemands: a list of integers, each denotes the cargo demand of a customer
        DepotLocations: a list of lists, each sublist contains two floats for coordinates of a distribution center
        CustomerLocations: a list of lists, each sublist contains two floats for coordinates of a customer's location
        CustomerTimeWindows: a list of lists, each sublist contains two integers for start/end of customer's delivery time window

    Returns:
        TotalOperationalTime: an integer, denotes the minimized total operational time (travel + service) for all vehicles
    """
    # To be implemented
    TotalOperationalTime = 1e9
    return TotalOperationalTime